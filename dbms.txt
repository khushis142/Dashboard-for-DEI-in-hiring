CREATE DATABASE ALWAYS_FIRST_IT_ENABLED_SOLUTIONS;
USE ALWAYS_FIRST_IT_ENABLED_SOLUTIONS;

CREATE TABLE EMPLOYEE(
EMPLOYEE_ID INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
FNAME VARCHAR(20) NOT NULL ,
LNAME VARCHAR(20) NOT NULL,
GENDER CHAR(1) NOT NULL ,
CHECK (GENDER IN ('M', 'F')),
DEPARTMENT_ID INT NOT NULL,
BUSINESS_UNIT VARCHAR(20),
/*CHECK (BUSINESS_UNIT IN('MANUFACTURING', 'HEALTHCARE', 'BANKING', 'LOGISTICS')),*/
SALARY INT NOT NULL,
HIRING_DATE DATE NOT NULL,
POSITION VARCHAR(20) NOT NULL,
CHECK (POSITION IN('ANALYST', 'ASSOCIATE', 'VP', 'ED', 'MD')),
FOREIGN KEY (DEPARTMENT_ID)REFERENCES DEPARTMENTS(DEPARTMENT_ID)
);


CREATE TABLE CANDIDATE (
CANDIDATE_ID INT NOT NULL AUTO_INCREMENT,
CANDIDATE_FNAME VARCHAR(30) NOT NULL,
CANDIDATE_LNAME VARCHAR(30) NOT NULL,
GENDER CHAR(1) NOT NULL ,
CHECK (GENDER IN ('M', 'F')),
DEPARTMENT_ID INT(30) NOT NULL,
APPLIED_POSITION VARCHAR(20) NOT NULL,
CHECK (APPLIED_POSITION IN('ANALYST', 'ASSOCIATE', 'VP', 'ED', 'MD')),
APPLIED_THROUGH VARCHAR(20) NOT NULL,
CHECK (APPLIED_THROUGH IN('VENDOR', 'CAMPUS', 'REFERENCE','WEBSITE')),
STATUS VARCHAR(20) NOT NULL,
CHECK (STATUS IN('STAGE 1', 'STAGE 2', 'HIRED')),
EXPERIENCE_YEARS INT NOT NULL,
VENDOR_ID INT ,
CAMPUS_ID INT,
/*EMP_ID_REFERER,*/
APPLICATION_DATE DATE NOT NULL,
PRIMARY KEY (CANDIDATE_ID),
FOREIGN KEY (VENDOR_ID) REFERENCES VENDORS(VENDOR_ID),
FOREIGN KEY (CAMPUS_ID) REFERENCES CAMPUS(CAMPUS_ID),
FOREIGN KEY (DEPARTMENT_ID) REFERENCES DEPARTMENTS(DEPARTMENT_ID)
);




CREATE TABLE VENDORS(
    VENDOR_ID INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    VENDOR_NAME VARCHAR(30) NOT NULL
    );

CREATE TABLE DEPARTMENTS(
    DEPARTMENT_ID INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    DEPARTMENT_NAME VARCHAR(30) NOT NULL
    );

 

CREATE TABLE CAMPUS(
    CAMPUS_ID INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    CAMPUS_NAME VARCHAR(30) NOT NULL,
    CAMPUS_TYPE VARCHAR(10) NOT NULL ,
    CHECK (CAMPUS_TYPE IN('BOYS','GIRLS','COED'))    
    );
    
CREATE TABLE AVAILABLE_JOBS(
JOB_ID INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
DEPARTMENT_ID INT NOT NULL,
POSITION VARCHAR(20) NOT NULL,
CHECK (POSITION IN('ANALYST', 'ASSOCIATE', 'VP', 'ED', 'MD')), 
BUSINESS_UNIT VARCHAR(20),
CREATION_DATE DATE NOT NULL,
FOREIGN KEY (DEPARTMENT_ID)REFERENCES DEPARTMENTS(DEPARTMENT_ID)

);

INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (1,'ANALYST','BANKING','2020-07-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (2,'VP','MANUFACTURING','2020-09-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (3,'ASSOCIATE','HEALTHCARE','2020-09-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (4,'ED','BANKING','2020-09-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (5,'MD','HEALTHCARE','2021-09-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (2,'ANALYST','BANKING','2021-07-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (3,'VP','MANUFACTURING','2021-09-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (4,'ASSOCIATE','HEALTHCARE','2021-09-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (5,'ED','BANKING','2022-09-05');
INSERT INTO `AVAILABLE_JOBS` (`DEPARTMENT_ID`, `POSITION`, `BUSINESS_UNIT`, `CREATION_DATE`) 
VALUES (1,'VP','HEALTHCARE','2022-09-05');



INSERT INTO DEPARTMENTS(DEPARTMENT_NAME)
VALUES("SALES");
INSERT INTO DEPARTMENTS(DEPARTMENT_NAME)
VALUES("FINANCE");
INSERT INTO DEPARTMENTS(DEPARTMENT_NAME)
VALUES("IT");
INSERT INTO DEPARTMENTS(DEPARTMENT_NAME)
VALUES("HR");
INSERT INTO DEPARTMENTS(DEPARTMENT_NAME)
VALUES("ADMINISTRATION");

INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR1");
INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR2");
INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR3");
INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR4");
INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR5");
INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR6");
INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR7");
INSERT INTO VENDORS(VENDOR_NAME)
VALUES("VENDOR8");

INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS1","BOYS");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS2","GIRLS");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS3","COED");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS4","COED");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS5","COED");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS6","GIRLS");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS7","BOYS");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS8","BOYS");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS9","COED");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS10","BOYS");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS11","GIRLS");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS12","COED");
INSERT INTO CAMPUS(CAMPUS_NAME,CAMPUS_TYPE)
VALUES("CAMPUS13","COED");


INSERT INTO `CANDIDATE` (`CANDIDATE_FNAME`, `CANDIDATE_LNAME`, `GENDER`, `DEPARTMENT_ID`, `APPLIED_POSITION`, `APPLIED_THROUGH`, `STATUS`, `EXPERIENCE_YEARS`, `CAMPUS_ID`, `APPLICATION_DATE`)
VALUES ('ALICE','ARNOLD','F',1,'ANALYST','CAMPUS','STAGE 1',3,3,'2020-06-15');


INSERT INTO EMPLOYEE(FNAME,LNAME,GENDER,DEPARTMENT_ID,BUSINESS_UNIT,SALARY,HIRING_DATE,POSITION)
VALUES ('BRAD','BON','M',1,'BANKING',10000,'2020-01-03','ANALYST');




---------------------------------------------------

INSERT INTO DEPARTMENTS (DEPARTMENT_NAME) VALUES
    ('Sales'),
    ('IT'),
    ('Finance'),
    ('HR'),
    ('Operations');

INSERT INTO VENDORS (VENDOR_NAME) VALUES
    ('Vendor A'),
    ('Vendor B'),
    ('Vendor C'),
    ('Vendor D'),
    ('Vendor E');

INSERT INTO CAMPUS (CAMPUS_NAME, CAMPUS_TYPE) VALUES
    ('Campus 1', 'BOYS'),
    ('Campus 2', 'GIRLS'),
    ('Campus 3', 'COED'),
    ('Campus 4', 'BOYS'),
    ('Campus 5', 'GIRLS'),
    ('Campus 6', 'COED'),
    ('Campus 7', 'BOYS'),
    ('Campus 8', 'GIRLS'),
    ('Campus 9', 'COED'),
    ('Campus 10', 'COED');



INSERT INTO EMPLOYEE (FNAME, LNAME, GENDER, DEPARTMENT_ID, BUSINESS_UNIT, SALARY, HIRING_DATE, POSITION)
SELECT
    CONCAT('Employee', t1.num),
    CONCAT('Lastname', t1.num),
    CASE WHEN RAND() < 0.73 THEN 'M' ELSE 'F' END,
    CEIL(RAND() * 5),
    CASE
        WHEN RAND() < 0.25 THEN 'MANUFACTURING'
        WHEN RAND() < 0.5 THEN 'HEALTHCARE'
        WHEN RAND() < 0.75 THEN 'BANKING'
        ELSE 'LOGISTICS'
    END,
    FLOOR(RAND() * 5000) + 3000,
    DATE_ADD('2016-01-01', INTERVAL CEIL(RAND() * 2000) DAY),
    CASE
        WHEN RAND() < 0.2 THEN 'ANALYST'
        WHEN RAND() < 0.4 THEN 'ASSOCIATE'
        WHEN RAND() < 0.6 THEN 'VP'
        WHEN RAND() < 0.8 THEN 'ED'
        ELSE 'MD'
    END
FROM
    (SELECT @num := @num + 1 AS num
    FROM
        (SELECT 0 UNION ALL SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3) AS a
        CROSS JOIN
        (SELECT 0 UNION ALL SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3) AS b
        CROSS JOIN
        (SELECT 0 UNION ALL SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3) AS c
        CROSS JOIN
	(SELECT 0 UNION ALL SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3) AS d
        CROSS JOIN
        (SELECT @num := 0) as e) AS t1
/* WHERE
    t1.num <= 100; */
LIMIT 100;








INSERT INTO candidate (CANDIDATE_FNAME, CANDIDATE_LNAME, GENDER, DEPARTMENT_ID, APPLIED_POSITION, APPLIED_THROUGH, STATUS, EXPERIENCE_YEARS,VENDOR_ID, CAMPUS_ID, APPLICATION_DATE)
SELECT FNAME, LNAME, GENDER, DEPARTMENT_ID, POSITION, 
    @appliedThrough := CASE
	WHEN RAND() < 0.25 THEN 'CAMPUS'
        WHEN RAND() < 0.5 THEN 'VENDOR'
        WHEN RAND() < 0.75 THEN 'REFERENCE'
        ELSE 'WEBSITE'
END,
    'HIRED',
    CEIL(RAND() * 20),
    CASE
	WHEN @appliedThrough = 'VENDOR' THEN CEIL(RAND() * 5)
	ELSE NULL
    END,
    CASE
	WHEN @appliedThrough = 'CAMPUS' THEN CEIL(RAND() * 10)
	ELSE NULL
    END,
    HIRING_DATE
FROM EMPLOYEE;




/*delimiter //
CREATE FUNCTION getAppliedThrough (random decimal)
RETURNS VARCHAR(20)
DETERMINISTIC
begin

DECLARE appliedThrough varchar(20);
if random < 0.25 then SET appliedThrough = 'VENDOR';
elseif random < 0.5 then SET appliedThrough = 'CAMPUS';
elseif random < 0.75 then SET appliedThrough = 'REFERENCE';
else SET appliedThrough = 'WEBSITE';
end if;

return appliedThrough;

END //

delimiter ;

set @appliedThrough = getAppliedThrough (RAND());*/











DELIMITER //
CREATE PROCEDURE InsertCandidates()
BEGIN

/*Variables*/
SET @i = 1;
SET @maxRows = 100;

/*Loop to insert rows*/
WHILE @i <= @maxRows
DO
  SET @fname = CONCAT('Candidate', @i);
  SET @lname = CONCAT('Lastname', @i);
  SET @gender = CASE WHEN @i % 2 = 0 THEN 'F' ELSE 'M' END;
  SET @departmentId = CEIL(RAND() * 5);
  SET @appliedPosition = CASE
        WHEN RAND() < 0.3 THEN 'ANALYST'
        WHEN RAND() < 0.5 THEN 'ASSOCIATE'
        WHEN RAND() < 0.7 THEN 'VP'
        WHEN RAND() < 0.8 THEN 'ED'
        ELSE 'MD'
    END;
  SET @appliedThrough = CASE
	WHEN RAND() < 0.25 THEN 'CAMPUS'
        WHEN RAND() < 0.5 THEN 'VENDOR'
        WHEN RAND() < 0.75 THEN 'REFERENCE'
        ELSE 'WEBSITE'
END;
  SET @status = CASE WHEN RAND() < 0.5 THEN 'STAGE 1' ELSE 'STAGE 2' END;
  SET @experienceYears = CEIL(RAND() * 20);
  set @vendorId = CASE
	WHEN @appliedThrough = 'VENDOR' THEN CEIL(RAND() * 5)
	 
	ELSE NULL
    END;
  set @campusId = CASE
	WHEN @appliedThrough = 'CAMPUS' THEN CEIL(RAND() * 10)
	ELSE NULL
    END;
  set @applicationDate = DATE_ADD('2016-01-01', INTERVAL CEIL(RAND() * 2000) DAY);

 /*Insert query*/
  INSERT INTO candidate (CANDIDATE_FNAME, CANDIDATE_LNAME, GENDER, DEPARTMENT_ID, APPLIED_POSITION, APPLIED_THROUGH, STATUS, EXPERIENCE_YEARS, VENDOR_ID, CAMPUS_ID, APPLICATION_DATE)
  VALUES (@fname, @lname, @gender, @departmentId, @appliedPosition, @appliedThrough, @status, @experienceYears, @vendorId, @campusId, @applicationDate);

  /*Increment counter*/
  SET @i = @i + 1;
END WHILE;
END//

DELIMITER ;

CALL InsertCandidates();








INSERT INTO candidate (CANDIDATE_FNAME, CANDIDATE_LNAME, GENDER, DEPARTMENT_ID, APPLIED_POSITION, APPLIED_THROUGH, STATUS, EXPERIENCE_YEARS,VENDOR_ID, CAMPUS_ID, APPLICATION_DATE)
SELECT FNAME, LNAME, GENDER, DEPARTMENT_ID, POSITION, 
    @appliedThrough := CASE
	WHEN RAND() < 0.25 THEN 'CAMPUS'
        WHEN RAND() < 0.5 THEN 'VENDOR'
        WHEN RAND() < 0.75 THEN 'REFERENCE'
        ELSE 'WEBSITE'
END,
    'HIRED',
    CEIL(RAND() * 20),
    CASE
	WHEN @appliedThrough = 'VENDOR' THEN CEIL(RAND() * 5)
	ELSE NULL
    END,
    CASE
	WHEN @appliedThrough = 'CAMPUS' THEN CEIL(RAND() * 10)
	ELSE NULL
    END,
    HIRING_DATE
FROM EMPLOYEE;
